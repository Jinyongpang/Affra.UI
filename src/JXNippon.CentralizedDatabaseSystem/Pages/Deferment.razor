@page "/deferment/{activeKey}"
@using JXNippon.CentralizedDatabaseSystem.Shared.Views
@attribute [Authorize]
<PageTitle>Deferment</PageTitle>

<Tabs ActiveKey="@activeKey" DefaultActiveKey="@activeKey" @ref=tabs OnTabClick=OnActiveKeyChanged>
    <TabPane Key="dashboard">
        <TabTemplate>
            <span>Dashboard</span>
        </TabTemplate>
        <ChildContent><ViewTab Page="Deferment"></ViewTab></ChildContent>
    </TabPane>
    <TabPane Key="report">
        <TabTemplate>
            <span>Report</span>
        </TabTemplate>
        <ChildContent>Report</ChildContent>
    </TabPane>
</Tabs>
