@typeparam TItem

@if(this.Items is null
    || this.Items.Count == 0)
{
    if (ShowEmpty)
    {        
      <Empty></Empty>
    }
}
else
{
    @foreach(var context in this.Items)
    {
        @this.ChildContent(context)
    }
}

