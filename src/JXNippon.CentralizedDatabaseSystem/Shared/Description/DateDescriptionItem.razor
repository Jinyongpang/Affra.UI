<DescriptionsItem Title="@(stringLocalizer[Title])" Span=@(Span ?? 1)>
    @if (this.IsEditing)
    {
        <RadzenDatePicker @bind-Value=@this.Value DateFormat="d"></RadzenDatePicker>
    }
    else
    {
        @(this.Value.ToDateString())
    }
</DescriptionsItem>
     